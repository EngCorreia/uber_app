

enum BtnType {outline,text, elevated}
enum SnackbarType {warning, success, notification}
enum BottomSheetType {login, ratings, callCenter,deleteAccount,
  dataPicker,payment,addSchedule,invoiceGraph,contractGraph,share}


